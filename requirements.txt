pypdf>=6.0
pillow>=10.0
python-docx>=1.1
python-pptx>=1.0
openpyxl>=3.1

# Optional: only needed for gdrive_index.py
google-api-python-client>=2.0
google-auth-oauthlib>=1.0
google-auth-httplib2>=0.2

# Optional: only needed for photo_tagger.py (Anthropic)
anthropic>=0.40

# Optional: only needed for gemini_photo_tagger.py (Vertex AI on GCP)
# Use google-genai (REST) instead of google-cloud-aiplatform (gRPC); the
# latter crashed with heap corruption under thread concurrency in Cloud Shell.
google-genai>=0.3
